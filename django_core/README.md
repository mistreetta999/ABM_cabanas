# Carpeta Django del proyecto

Esta carpeta queda como material Django del proyecto para mantener organizada la
estructura que se estaba armando.

La configuracion activa esta en `cabanas_project/` y las apps Django activas son:

- `cabanas_apps`
- `chatbot`

Los templates HTML estan en `Template/`.
